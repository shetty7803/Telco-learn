# Telco-learn
Cloud and DevOps
Installed Docker Desktop through browser and dont forget to enable the wsl2
Restart the system
open docker desktop and click the terminal and this command 
docker --version
docker run hello-world
after the go to the container and check if it comes there means properly working
go to the docker hub in docker desktop
search for a image which one you want and copy the commands
then open vs-code
create a folder
and create files
one with the code what you to want
another with Dockerfile in that image with workdirectory and image with file path
and last requriements file
save all the files
open terminal and run commands
docker build -t with image name
docker run with image name
docker images 
docker rm with container id
docker rmi with image name
docker ps = working processors
